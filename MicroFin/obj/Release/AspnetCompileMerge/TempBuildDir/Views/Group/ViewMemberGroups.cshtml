@model IEnumerable<MicroFin.Models.MemberGroup>

@{
    ViewBag.Title = "View Groups";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Groups</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GroupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CenterId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CenterName)
        </th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GroupId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CenterId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CenterName)
        </td>
        <td>
            @Html.ActionLink("Members", "ViewMembers", "Member", new { id = item.GroupId },null)
        </td>
        <td>
            @Html.ActionLink("Loans", "ViewLoans", "Loan", new { id = item.GroupId }, null)
        </td>
        <td>
            @Html.ActionLink("TrasferReport", "LoanTransferReport", "Loan", new { id = item.GroupId }, null)
        </td>
        <td>
            @Html.ActionLink("LoanStatus", "LoanRepaymentStatus", "Loan", new { id = item.GroupId }, null)
        </td>
        <td>
            @Html.ActionLink("Edit", "MemberGroupForm", new { id=item.GroupId })
        </td>
    </tr>
}

</table>
